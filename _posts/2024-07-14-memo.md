---
title: "2024-07"
---
<pre><code>
template<typename T>
class LockGuard {
public:
	LockGuard(T& m) {
		_mutex = &m;
		_mutex->lock();
	}

	LockGuard() {
		_mutex->unlock();
	}
private:
	T* _mutex;
};
  }</code></pre>
